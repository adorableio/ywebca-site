# YWebCA Website

### Install requirements
```shell
npm install
```

### Running things
```shell
# starts the app in the browser with browsersync
$(npm bin)/gulp serve
```

### Deploy
```shell
$(npm bin)/gulp deploy
```

For the [YWebCA](http://ywebca.org/).
